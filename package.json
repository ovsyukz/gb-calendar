{
  "name": "gb-calendar",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Tournament calendar for Gracie Barra Deerfield",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "netlify dev",
    "migrate": "node scripts/migrate.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0",
    "@netlify/neon": "^0.1.0"
  },
  "devDependencies": {
    "eslint": "^9.15.0",
    "globals": "^15.12.0",
    "prettier": "^3.4.0",
    "vitest": "^4.1.10"
  }
}
