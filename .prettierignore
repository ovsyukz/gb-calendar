# Generated by the build — formatting them achieves nothing, and the next
# build overwrites the result.
public/index.html
public/styles.css

# A fragment, not a document. It opens with an include that supplies
# <!doctype>, <html> and <head>, so its closing tags have no opening tag in
# this file and Prettier cannot parse it standalone. The partials it pulls in
# are ordinary fragments and are formatted normally.
src/index.html

# Local development database.
.pgdata/
